Hi this is text file from the desktop that I want to commit to my github repo.


How to github

Create an empty repo
folder in local
Initiate git by git Init
make changes
add that to git by git add .
commit to git by git commit -m "comment here"
git remote add origin link *for your repo*
push the file by git push -u origin master